import { ServicesView } from "@/components/services/services-view";

export const metadata = {
  title: "Вет/Груминг — ЛапМаркет",
};

export default function ServicesPage() {
  return <ServicesView />;
}
